name: Build Android Release and Test APK

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android API 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Restore approved launcher artwork
        run: |
          mkdir -p app/src/main/res/drawable-nodpi
          base64 --decode launcher-icon.png.b64 > app/src/main/res/drawable-nodpi/masterpiece_icon.png

      - name: Build test APK and release bundle
        run: |
          set +e
          gradle --no-daemon :app:assembleDebug :app:bundleRelease > android-build.log 2>&1
          result=$?
          cat android-build.log
          exit $result

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-build-diagnostics
          path: android-build.log
          if-no-files-found: warn

      - name: Verify test APK
        run: |
          cp app/build/outputs/apk/debug/app-debug.apk Masterpiece-Wallpapers-AI1-v1.6-test.apk
          apksigner verify --verbose Masterpiece-Wallpapers-AI1-v1.6-test.apk

      - name: Restore encrypted upload keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/upload-key.jks"

      - name: Sign and verify release bundle
        env:
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          cp app/build/outputs/bundle/release/app-release.aab Masterpiece-Wallpapers-AI1-v1.6-signed.aab
          jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
            -keystore "$RUNNER_TEMP/upload-key.jks" \
            -storepass "$KEYSTORE_PASSWORD" \
            -keypass "$KEYSTORE_PASSWORD" \
            Masterpiece-Wallpapers-AI1-v1.6-signed.aab masterpiece-upload
          jarsigner -verify -verbose -certs Masterpiece-Wallpapers-AI1-v1.6-signed.aab

      - name: Upload installable test APK
        uses: actions/upload-artifact@v4
        with:
          name: masterpiece-ai1-v1.6-test-apk
          path: Masterpiece-Wallpapers-AI1-v1.6-test.apk
          if-no-files-found: error

      - name: Upload signed Play Store bundle
        uses: actions/upload-artifact@v4
        with:
          name: masterpiece-ai1-v1.6-release-aab
          path: Masterpiece-Wallpapers-AI1-v1.6-signed.aab
          if-no-files-found: error
